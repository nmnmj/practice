/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     appDir: true,
    //     serverComponentsExternalPackages: ["mongoose"],
    //     serverActions: true
    // },
    // serverComponentsExternalPackages: ["mongoose"],
    // serverAction: true,
    images:{
        domains:[""]
    }
}

module.exports = nextConfig
